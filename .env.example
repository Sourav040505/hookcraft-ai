# HookCraft AI - Environment Variables Example

# Replace with your actual Gemini API key to transition from mock data to live AI generations.
# Vite requires environment variables to start with "VITE_" prefix to be exposed to the client-side code.
VITE_GEMINI_API_KEY=your_gemini_api_key_here
